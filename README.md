# Anviksha Analytics UI Refresh

Drop-in replacement for the redesigned academic analytics UI.

Extract this ZIP into the Anviksha project root and allow existing files to be overwritten.

Replaced:
- src/app/globals.css
- src/components/AcademicPromotionCard.tsx
- src/components/PlacementEligibilityCard.tsx
- src/components/ReappearSessionPlanner.tsx

The redesign keeps the existing component APIs and grade-system logic, while refreshing spacing, hierarchy, surfaces, typography usage, status treatments, and card density.
